{
  "name": "android-messages-electron",
  "version": "1.0.1",
  "description": "Electron App for Android Messages",
  "main": "./app/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx electron ./app",
    "build": "npx electron-builder -m --config ./build/build.json"
  },
  "author": "AFreidz",
  "license": "ISC",
  "dependencies": {
    "electron": "^2.0.3",
    "electron-builder": "^20.16.0"
  }
}
